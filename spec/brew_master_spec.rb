# frozen_string_literal: true

RSpec.describe BrewMaster do
  it "has a version number" do
    expect(BrewMaster::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
